#include <stdio.h>
#include <stdlib.h>



int main(void) {
	//void qsort(void* ptr, size_t count, size_t size, int(*comp)(const void*, const void*));
	//ptr - array base address, count - array element count, size - each element bytes size, function);

	return 0;
}
